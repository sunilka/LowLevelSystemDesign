package StrategyDesignPattern;

// This is the interface that would hold the method that would be common between all the strategies.
public interface Strategy {
    public int calculate(int no1, int no2);
}
